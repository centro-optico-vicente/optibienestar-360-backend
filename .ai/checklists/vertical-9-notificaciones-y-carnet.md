# Alcance 9 — Notificaciones y Carnet digital

> Cola persistente de emails + jobs diarios de vencimientos + QR del afiliado.
> Índice: [../checklist.md](../checklist.md)

<!-- resumen-totales:start -->
| Tareas | Hechas | Pendientes | % avance | Estado |
|---|---|---|---|---|
| 10 | 10 | 0 | 100% | ✅ |

_Snapshot — recontar con `grep -c '^- \[x\]'`. Panorama global: [checklist.md](../checklist.md)._
<!-- resumen-totales:end -->

## Migraciones

- [x] [P0/C2] `V28__notifications.sql` _(Cola persistente outbound. **Por qué cola vs `@Async` fire-and-forget**: sobrevive restart JVM (no se pierde mail enqueueado en deploy), retry policy con exponential backoff visible en la row, audit trail per-recipient + per-source, admin queue UI puede mostrar "qué falló, por qué, reintentarlo". **Schema** (24 columns): channel CHECK 1-valor (EMAIL v1, extensible a SMS/PUSH con ALTER + CHECK update sin rewrite); recipient snapshot (`recipient_email CITEXT NOT NULL` capturado al enqueue para que cambios posteriores de email NO redirijan mail ya queueado + `recipient_user_id BIGINT NULL` FK opcional para navegabilidad + drill-downs + endpoint VIEW_OWN); locale (`recipient_locale VARCHAR(10)` default 'es' — pickea variante `*_es.html`/`*_en.html`); content (`template_code VARCHAR(80)` open sin CHECK whitelist — mismo patrón que `scheduled_jobs.code`, resuelve a Thymeleaf en classpath al render + nuevos templates landean por archivo no por migración; `subject VARCHAR(200)` pre-rendered al enqueue desde i18n bundle stored inline para replay sin re-resolver message keys; `template_vars JSONB` per-template para que cada template carry su shape — Hibernate 6 mapea con `@JdbcTypeCode(SqlTypes.JSON)` sobre `Map<String,Object>`); origin (`source_module VARCHAR(40)` + `source_entity_uuid UUID` — composite con `template_code` powers el idempotency check "ya enqueueamos `payment-approved` para este payment uuid?" sin necesidad de columna dedicada `idempotency_key`); lifecycle (status CHECK 5-valores: PENDING/SENDING/SENT/FAILED/DEAD_LETTER; `scheduled_for TIMESTAMPTZ` default NOW para future scheduled-at sends); retry policy per-row (`max_attempts` configurable: critical one-shot → 1, recordatorio rutinario → 5 service default; `attempt_count`, `last_attempt_at`, `next_retry_at` para exponential backoff 60s×2^(attempt-1) clamped a 1h, `last_error_message TEXT`); delivery outcome (`sent_at`); audit BaseEntity. **4 CHECK coherence**: (1) SENT ⇒ sent_at NOT NULL; (2) FAILED/DEAD_LETTER ⇒ last_error_message NOT NULL; (3) attempt_count ≤ max_attempts; (4) DEAD_LETTER ⇒ attempt_count ≥ max_attempts. **5 índices partial** (todos `WHERE` filtered para mantener chicos a escala — SENT + DEAD_LETTER son el bulk de la tabla a largo plazo): (a) `idx_notifications_pending_due (scheduled_for) WHERE status='PENDING' AND is_active` worker poll; (b) `idx_notifications_retry_due (next_retry_at) WHERE status='FAILED' AND next_retry_at NOT NULL AND is_active` retry poll separado del PENDING para no competir scans; (c) `idx_notifications_recipient_created (recipient_user_id, created_at DESC)` admin drill + VIEW_OWN; (d) `idx_notifications_source (source_module, source_entity_uuid, template_code)` pre-check idempotente al enqueue; (e) `idx_notifications_dead_letter (created_at) WHERE status='DEAD_LETTER'` triage admin del backlog. **Trigger** `set_updated_at()` reutiliza V2. **Permission catalog extendido**: dominio `NOTIFICATIONS` icono `i-lucide-bell` order 120; 3 perms `NOTIFICATION_VIEW_ALL`/`RESEND`/`VIEW_OWN`. **Distribución de grants** mirror exacto de `REFERRAL_CODE_VIEW_OWN` V6: SYSTEM + ADMINISTRADOR → 3 perms; AFILIADO + PROMOTOR + ALIADO → solo VIEW_OWN; OPERADOR* excluidos (actúan en nombre de otros, no reciben notificaciones personales). **Validación schema** corrida contra Postgres 15 real (DB ephemeral con V1+V2+V5+V6+V28 — skip V3/V7 que usan Flyway placeholders): 24 columns con tipos correctos (channel VARCHAR + recipient_email USER-DEFINED=CITEXT + template_vars JSONB), 5 índices partial visibles en pg_indexes, los 4 CHECK fire correctamente (SENT sin sent_at → reject, FAILED sin error → reject, DEAD_LETTER sin retries exhausted → reject, attempt_count > max → reject) + channel='SMS' → reject; happy paths (PENDING minimal, SENT con sent_at, DEAD_LETTER con 5/5) → accept. Local test profile sigue con Flyway disabled (H2 no parsea V1 extensions), CI corre full migration chain.)_
- [x] [P0/C2] `V29__digital_cards_view.sql` _(Creada como **`V39__digital_cards_view.sql`** — la numeración del checklist quedó stale (la migración más alta ya era V38). Vista `digital_cards_view`: una fila por member activo uniendo `persons` (full_name/documento), la membresía activa + plan (V21 partial UNIQUE ⇒ ≤1) y la cuenta `users` (1:1 con persons) para resolver la tarjeta por el usuario autenticado en un solo lookup. Mapeada por la entidad read-only `@Immutable DigitalCardRow`; el QR se genera en la app.)_

## Código

- [x] [P1/C2] Cola async `@Async` + reintentos exponenciales (Tarea 1.8 pendiente) _(El envío ya era `@Async`+`@Retryable` en `EmailService`; la cola persistente V28 le pone durabilidad encima. `EmailService.sendTemplatedSync` (nueva ruta síncrona, sin `@Async`/`@Retryable`) da al worker un punto de entrada que propaga `MailException`, para que la cola controle su propio backoff en vez de fire-and-forget. Backoff exponencial `60s × 2^(intento-1)` clamped a 1h, hasta `max_attempts` por fila; el último intento fallido → `DEAD_LETTER`. Cubierto por `NotificationServiceTest.backoff_*`.)_
- [x] [P0/C2] Entidad Notification (cola persistente) _(`modules/notification/entity/Notification` mapea las 24 columnas de V28: channel/recipient snapshot/template_code+subject+`template_vars` JSONB (`@JdbcTypeCode(SqlTypes.JSON)`)/source (module+entity_uuid)/lifecycle (status vía BaseEntity)/retry policy (attempt_count, max_attempts, next_retry_at, last_error_message)/sent_at. `recipient_user_id` como `Long` plano (sin `@ManyToOne`) para desacoplar del módulo auth. Enums internos `Channel{EMAIL}` + `NotificationStatus{PENDING,SENDING,SENT,FAILED,DEAD_LETTER}`. `NotificationRepository` con `findDueForDispatch` (PENDING vencidos + FAILED con backoff cumplido) y el pre-check idempotente por (module, entity, template).)_
- [x] [P0/C3] `NotificationService`: enqueue + envío async + reintentos _(2 responsabilidades: **enqueue** (comando `NotificationEnqueueCommand`, con dedup opcional por source) y **dispatchDue(batch)** (worker: reclama el lote vencido, renderiza+envía por `EmailService.sendTemplatedSync`, y marca SENT / FAILED+backoff / DEAD_LETTER por fila). Un fallo por destinatario nunca aborta el lote (cada envío va envuelto). Corre en una transacción para que el estado final de cada fila haga flush al commit. El worker se dispara desde `NotificationDispatchJobRunner`. Tests: `NotificationServiceTest` (enqueue idempotente, SENT, FAILED-con-retry, DEAD_LETTER, curva de backoff).)_
- [x] [P0/C2] Templates: `welcome.html`, `payment-reminder.html`, `payment-overdue.html`, `membership-suspended.html`, `membership-expired.html`, `referral-reward.html`, `commission-payout.html` _(Nuevas: `welcome`, `payment-reminder`, `payment-overdue`, `referral-reward` (×2 `_es`/`_en`, mismo estilo inline que las existentes). `membership-suspended`/`membership-expired`/`commission-payout` ya existían. 4 subjects i18n nuevos en los 3 bundles (`email.welcome/payment.reminder/payment.overdue/referral.reward.subject`). `welcome` se encola desde `MembersService.create` (idempotente por member); `payment-reminder`/`payment-overdue` los encolan los jobs; `referral-reward` queda como template listo para la emisión de recompensas.)_
- [x] [P0/C3] Job diario — detecta vencimientos próximos (3 días antes de `next_due_date`) _(`MembershipDueSoonJobRunner` (code `MEMBERSHIP_DUE_SOON`, seed V40, cron diario 06:00): match exacto `next_due_date == today+3` sobre membresías ACTIVE (`findByActiveTrueAndStatusAndNextDueDate`), encola `payment-reminder` vía `MembershipReminderEnqueuer`. Exact-day ⇒ un recordatorio por ciclo. La entrega es asíncrona por el worker de la cola.)_
- [x] [P0/C3] Job diario — detecta vencidos en período de gracia _(`MembershipGraceJobRunner` (code `MEMBERSHIP_GRACE`, seed V40, cron diario 06:30 tras el status sweep de 03:00): sobre las membresías SUSPENDED filtra el "día de aviso" por fila `next_due_date + max(1, gracePeriodDays-3)` (~3 días antes de expirar, clamped) y encola `payment-overdue` — distinto del aviso one-shot `membership-suspended` que el status sweep ya mandó. Tests: `MembershipGraceJobRunnerTest` (filtro de día de aviso) + `MembershipReminderEnqueuerTest`.)_
- [x] [P0/C2] `GET /v1/me/digital-card` + QR generado _(`MyDigitalCardController` gated `MEMBER_VIEW_OWN`; `DigitalCardService` lee `DigitalCardRow` (vista V39) por `user_uuid` (404 `me.member.not_enrolled` si no es afiliado) y genera el QR con **ZXing** (Apache-2.0, `QrCodeGenerator`) como PNG `data:` URI inline (sin R2). El QR codifica el member uuid para que un aliado lo escanee en el validador. Tests: `QrCodeGeneratorTest`, `DigitalCardServiceTest`, `MyDigitalCardControllerIT`.)_
- [x] [P0/C2] `GET /v1/me/family` + `/v1/me/usage-history` _(`MyFamilyController` (`/v1/me/family`, `MEMBER_VIEW_OWN`) → `BeneficiariesService.listForUser` (resuelve el member por `user.person → member.person`, 404 si no enrolado, lista sus beneficiarios activos). `MyUsageHistoryController` (`/v1/me/usage-history`, `MEMBER_VIEW_OWN`, sort `usageDate` DESC) → `BenefitUsagesService.listForMemberUser` con `BenefitUsageRepository.findByMemberUserUuid` (feed de consumo del afiliado a través de la cadena membership→benefit_usage). Test: `BeneficiariesServiceTest.listForUser_*`.)_
