# Alcance 6 — Pagos manuales

> Flujo: registro → revisión humana → aprobación/rechazo + email + comisión.
> Índice: [../../checklist-vertical.md](../../checklist-vertical.md)

## Migraciones

- [x] [P0/C3] `V23__payments.sql` _(Schema: `membership_id` FK + `payer_user_id` FK NULL (cash at counter sin user) + money (`amount NUMERIC(10,2) > 0` + `currency` default USD) + `payment_method` CHECK ENUM 7 valores (BANK_TRANSFER/CASH/ZELLE/PAGO_MOVIL/CRYPTO/INTERNATIONAL_TRANSFER/OTHER) + `reference_number` + `payment_date` (settlement bank) + `received_at` (system) + **allocation** (`inscription BOOLEAN` + `applied_period DATE` primer día del mes para recurring) + **proof of payment** (R2 key + nombre + content_type + size_bytes) + `admin_notes` + **review workflow** (`reviewed_by BIGINT FK users + reviewed_at + review_reason TEXT`) + status CHECK (PENDING/APPROVED/REJECTED) + audit BaseEntity. **CHECK constraints**: (1) `review_consistency` — PENDING ⇔ reviewed_at/by NULL; APPROVED|REJECTED ⇔ reviewed_at/by NOT NULL; (2) `rejection_has_reason` — REJECTED ⇒ review_reason NOT NULL; (3) `date_not_future` — payment_date ≤ today+1d (anti typo); (4) `inscription_no_period` — inscription ⇒ applied_period NULL (es one-time, no aligned a mes). **Índices**: `(status, received_at DESC) WHERE is_active` para cola pending del admin; `(membership_id, payment_date DESC)` para historial; `(applied_period) WHERE is_active AND status='APPROVED'` para reportes mensuales. **Reviewed_by usa BIGINT** (joins cheap, matchea V11 ally_services pattern). **Currency** queda en USD por default — VES y conversión FX rates van en v2. **FK diferido aplicado al final**: `ALTER TABLE beneficiaries ADD CONSTRAINT fk_beneficiaries_inscription_payment FOREIGN KEY (inscription_payment_id) REFERENCES payments(payments_id)` — ON DELETE NO ACTION (soft-delete es el patrón canónico). contextLoads V1..V23 limpio.)_
- [x] [P0/C2] Índice: `payments(status)` _(Implementado en V23 como índice composite partial **`idx_payments_status_received (status, received_at DESC) WHERE is_active`** — superior a un `(status)` plano: (a) Postgres usa la columna leading `status` para filtros por estado simple sin penalty, (b) el orden secundario `received_at DESC` evita un sort adicional cuando el admin abre la cola "pagos a revisar" (`WHERE status='PENDING' ORDER BY received_at DESC`), (c) el predicate `WHERE is_active` excluye soft-deleted del scan = índice más chico y caliente. V23 además incluye `idx_payments_membership_date (membership_id, payment_date DESC)` para historial por afiliado y `idx_payments_approved_period (applied_period) WHERE is_active AND status='APPROVED'` para reportes mensuales — los 3 cubren las queries típicas de vertical-6.)_

## Código

- [x] [P0/C2] Entidad Payment _(JPA entity en `modules/payment/entity/Payment.java`. `extends BaseEntity` con `@AttributeOverride id→payments_id`. **Relaciones**: `@ManyToOne` Membership (LAZY, optional=false; FK `membership_id`), `@ManyToOne` User payer (LAZY, NULL; FK `payer_user_id`), `@ManyToOne` User reviewer (LAZY, NULL; FK `reviewed_by`). **Money**: `BigDecimal amount` precision=10/scale=2, `String currency` default 'USD'. **Method + reference**: `@Enumerated(STRING) PaymentMethod paymentMethod` inner enum 7 valores (BANK_TRANSFER/CASH/ZELLE/PAGO_MOVIL/CRYPTO/INTERNATIONAL_TRANSFER/OTHER — matches V23 CHECK), `String referenceNumber`. **Dates**: `LocalDate paymentDate` (settlement bank), `Instant receivedAt` default ahora. **Allocation**: `boolean inscription` default false, `LocalDate appliedPeriod` (primer día del mes para recurring; nullable). **Proof of payment R2**: `supportFileUrl/Name/ContentType/SizeBytes`. **Review workflow**: `Instant reviewedAt`, `String reviewReason` (TEXT). **Status** (PENDING/APPROVED/REJECTED) hereda de BaseEntity como String — mismo patrón Membership.LifecycleStatus; expone enum interno `PaymentStatus` para type safety en service (`payment.setStatus(PaymentStatus.APPROVED.name())`). El CHECK V23 garantiza que solo los 3 valores entran al column. Hibernate validate verde — contextLoads OK con la entity nueva.)_
- [x] [P0/C3] `POST /v1/admin/payments` (multipart soporte → R2) _(`AdminPaymentController.POST` consume `multipart/form-data` con 2 parts: `payment` (JSON `PaymentCreateRequest` validado), `support` (MultipartFile opcional). Guardado por `PAYMENT_REGISTER`. 201 + Location. **`PaymentsService.register`** (plural — convención AlliesService/PlansService): resuelve membership (404 limpio si no existe), payerUser opcional (NULL = cash-at-counter), arranca con `status=PENDING`. **Allocation defaults**: si `inscription=true` → `appliedPeriod` forzado a NULL (mirror del CHECK V23 `inscription_no_period`); si `inscription=false` y vino NULL → default al primer día del mes actual (admin no tiene que retipear); si vino explícito → normalizado a primer día del mes. **R2 separado** del happy path: file metadata (name/contentType/size) siempre se persiste en el row aunque el upload no ocurra; `StorageService` inyectado via `ObjectProvider<StorageService>` (es `@ConditionalOnProperty storage.r2.enabled=true` — hoy off por default). Cuando R2 está off: log info + bytes descartados + `support_file_url=null`. Cuando R2 está on: upload a `payments/proofs/{uuid}-{safeName}` + URL persistido. Mismo código en ambos modos — solo cambio del env var. **DTOs**: PaymentCreateRequest con `@Digits(8,2)` + `@DecimalMin(0.01)` sobre amount; `@Pattern ISO 4217` sobre currency; `@NotNull PaymentMethod` enum; `@PastOrPresent` sobre paymentDate. PaymentDto flat con membership/member/plan refs extraídas + `supportFileAvailable` boolean (sin exponer URL raw — el endpoint /support de presigned URL viene en bullet siguiente). PaymentMapper MapStruct con `@Named isPresent` para el boolean. 4 keys i18n nuevas en 3 bundles. 83/83 tests verdes, contextLoads con beans nuevos.)_
- [x] [P0/C3] `PUT /v1/admin/payments/{id}/approve` _(Guardado por `PAYMENT_APPROVE`. Body opcional (`PaymentApproveRequest` con `reason` que puede ir como nota de aprobación). `PaymentsService.approve` valida que el pago esté **PENDING** (422 limpio `payment.review.not_pending` si ya fue revisado) — mirror del CHECK V23 `chk_payments_review_consistency` para preempt antes que dispare a nivel BD. Setea status=APPROVED + `reviewedBy` (resuelve User via UUID del actor del JWT) + `reviewedAt=NOW()` + reason opcional en review_reason. Devuelve `PaymentDto` con el estado actualizado. Body opcional via `@RequestBody(required=false)` + null-safe en service — admin puede curl sin payload.)_
- [x] [P0/C2] `PUT /v1/admin/payments/{id}/reject` _(Guardado por `PAYMENT_REJECT`. Body **requerido** (`PaymentRejectRequest` con `@NotBlank reason`) — el CHECK V23 `chk_payments_rejection_has_reason` requiere review_reason NOT NULL para REJECTED, y el afiliado necesita saber por qué para re-enviar. Misma validación PENDING que approve. Status=REJECTED + reviewedBy/At/reason. **Service compartido** `applyReview` en `PaymentsService` (approve y reject difieren solo en target status + obligatoriedad de reason). El método sigue Membership.LifecycleStatus pattern usando `Payment.PaymentStatus.{APPROVED,REJECTED}.name()` para type safety contra el CHECK V23. 2 keys i18n nuevas en 3 bundles. 83/83 tests verdes.)_
- [x] [P0/C2] `GET /v1/admin/payments` + RSQL _(Endpoint `GET /v1/admin/payments` guardado por `PAYMENT_VIEW_ALL`. Default `@PageableDefault(size=20, sort="receivedAt", DESC)` — la cola del admin canónicamente quiere "newest first by received_at", y combinado con `?filter=status==PENDING` aprovecha exactamente el índice V23 `(status, received_at DESC) WHERE is_active`. **RSQL filter** sobre 12 campos whitelisted (status, paymentMethod, currency, amount, inscription, paymentDate, receivedAt, appliedPeriod, reviewedAt, createdAt, updatedAt, active) — `RsqlFieldValidator` da 422 limpio si admin filtra por algo no permitido. **Free-text `?q`** sobre 3 campos (referenceNumber, adminNotes, supportFileName) vía `SearchSpecifications.acrossFields`. Combina con `activeOnly()` Specification base (excluye soft-deleted). 1 key i18n nueva `payment.filter.field_not_allowed` en 3 bundles. 83/83 tests verdes.)_
- [x] [P0/C2] `GET /v1/admin/payments/{id}/support` (presigned URL) _(`GET /v1/admin/payments/{uuid}/support` guardado por `PAYMENT_VIEW_ALL`. Response **JSON** `PaymentSupportUrlDto` (no 302 redirect — SPAs prefieren inspeccionar la URL + 302+Authorization tiene problemas conocidos cross-browser). Body: `url` + `expiresAt` + `expiresInSeconds` + `fileName`/`contentType`/`sizeBytes` (echo metadata para que el frontend renderice "Download proof.pdf (256 KB)" sin segundo round-trip). **Query param `?ttlMinutes=N`** opcional, clampeado [1..60], default 5min — controlled via `@Min(1) @Max(60)` en el path variable + clampTtl service-side por defensive depth. **Failure modes mapeados**: payment no existe → 404 `payment.not_found`; `support_file_url=NULL` (no se attacheó proof, o se registró cuando R2 estaba off) → 404 `payment.support.not_available`; `StorageService` bean no disponible (R2 disabled en la réplica) → 422 `payment.support.storage_unavailable`. **`StorageService` inyectado via `ObjectProvider`** (mismo patrón que el POST register) — cuando R2 está off el endpoint devuelve 422 limpio sin NPE. La presign llama `storage.generatePresignedUrl(key, duration)` directamente con la key del row `support_file_url`. 2 keys i18n nuevas en 3 bundles. 83/83 tests verdes.)_
- [x] [P0/C2] `GET /v1/me/payments` _(`MyPaymentsController.GET /v1/me/payments` guardado por `PAYMENT_VIEW_OWN`. Resuelve el current user via `@AuthenticationPrincipal CustomUserDetails actor.getUuid()`. **Single JPQL query** (`PaymentRepository.findOwnByUserUuid`) walks `payment → membership → member → person → user` matching por `user.uuid` con subquery — mismo patrón que `MemberRepository.findByUserUuid` que usa `MyMemberController`. Filtra `p.active = true` para no exponer soft-deleted. Default `@PageableDefault(size=20, sort="receivedAt" DESC)` — newest first como en el admin. **Sin RSQL ni free-text `?q`** — la vista del afiliado es full history, no curada query (esa es la del admin). Devuelve `Page<PaymentDto>` reutilizando el mapper que ya extrae plan/membership refs flat para que la app móvil/web renderice sin nested associations. Sin keys i18n nuevas. 83/83 tests verdes.)_
- [x] [P0/C2] Templates `payment-received.html` + `payment-approved.html` + `payment-rejected.html` _(**6 templates** Thymeleaf (3 × `_es.html` + `_en.html`): `payment-received` (azul `#1a56a0`, info-box "pendiente de revisión"), `payment-approved` (verde `#1f7a3f`, ok-box con plan/mes cubierto/inscripción label, eco de `reviewReason` como nota opcional del revisor), `payment-rejected` (rojo `#b22222`, alert-box + reason-box ámbar que destaca el motivo). Formatos de fecha por locale (`dd/MM/yyyy` ES, `yyyy-MM-dd` EN). Variables: `fullName`, `planName`, `amount`, `currency`, `paymentMethod`, `referenceNumber` (opcional), `paymentDate`, `inscription`, `appliedPeriod`, `reviewReason`. **3 subjects nuevos** en `messages*.properties` × 3 bundles = 9 keys: `email.payment.{received,approved,rejected}.subject`. **Wiring** desde `PaymentsService`: `register` dispara `payment-received` después del save; `approve` dispara `payment-approved` después del `applyReview`; `reject` dispara `payment-rejected`. **Failures de SMTP NO rollbackean la transacción** — el row mutation ya landed antes del dispatch; try/catch + log error → swallow (admin + afiliado pueden ver el resultado en la admin queue / `/v1/me/payments`). Helper `dispatchNotification(payment, template, subjectKey)` walks `payment → membership → member → person` para resolver email + locale (`Locale.forLanguageTag` con fallback "es"), skipea silencioso si person/email faltante (case cash-at-counter sin user). 83/83 tests verdes.)_
